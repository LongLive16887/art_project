const scrolling = () => {
    window.addEventListener('scroll', () => {
        if(document.documentElement.scrollTop > 1650){
            console.log(1);
        }
    });
};

/**  
    * dfsd
    **sjksdsk
    *! das 
    *?
    *TODO:  
    *@param
*/

export default scrolling;